#!/bin/bash

#source this in .bashrc to apply to open terminals

# Check for visual monitor
if [[ "$(tty)" == *"pts"* ]]; then
	source ~/.refactory/default.prompt
fi

# Prompt aliases
alias refactory="source ~/.refactory/default.prompt"


# Other aliases
source ~/.refactory/.aliases